# URL-Parsing
A backend application to parse, normalize, and store structured components of URLs. The system supports efficient data processing and retrieval, with an extensible architecture for future enhancements. Containerized using Docker for consistent deployments and scalable execution.
